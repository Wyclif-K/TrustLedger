# Dockerfile expects build context = Git repo root (COPY blockchain-core/… and ussd-bridge/…).
# Railway: Root Directory "/" or empty, Dockerfile path "blockchain-core/Dockerfile".
# Prevents Gradle/Android from hijacking deploy when root contains Android — Dockerfile lives under blockchain-core.
[build]
builder = "dockerfile"
dockerfilePath = "blockchain-core/Dockerfile"
